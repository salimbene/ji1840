import React from 'react';
import { Link, NavLink } from 'react-router-dom';

const UIHeader = ({ user, consortia }) => {
  return <div className="cc--ui-header">header</div>;
};

export default UIHeader;
